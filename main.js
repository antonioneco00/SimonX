/// <reference path="jQuery/index.d.ts"/>

$(document).ready(() => {
    var botonEmpezar = $("#botonEmpezar");
    var contenedorBotones = $("#contenedorBotones");
    var botones = $(".botones");
    botones.hide();
    // contenedorBotones.css('display', 'grid');
});